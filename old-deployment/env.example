# Required
SECRET_KEY=replace-with-a-long-random-secret
DATABASE_URL=postgresql://user:password@host:5432/database

# Set to 1 only when HTTPS is enabled (recommended for production).
SESSION_COOKIE_SECURE=1

# Local development only; keep 0 in production.
FLASK_DEBUG=0
